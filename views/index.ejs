<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
  <center>
    <h1>Bienvenido a <%= title %></h1>
    <p>La mejor radio de la ETSIT</p>
    <form methods="get" actions="/">
    	<input type="text" name="search" placeholder="Buscar en Youtube..."/>
    	<input type="submit" value="Buscar">
    </form>
    <% if (searchWords != "") { %>
    <table>
      <tr>
        <th colspan="3">Resultados de: <%= searchWords %></th>
    <% for (var key in body.items) { %>
      <tr>
        <td><%= body.items[key].snippet.title %> </td>
        <td><img src="<%= body.items[key].snippet.thumbnails.default.url%>" alt="Smiley face" height="<%= body.items[key].snippet.thumbnails.default.height%>" width="<%= body.items[key].snippet.thumbnails.default.width%>"></td>
        <td>
          <form methods="get" actions="/vd">
            <input type="hidden" name="vd" value="<%= body.items[key].id.videoId %>">
            <input type="submit" value="Añadir a lista de reproducción">
          </form>
        </td>
      </tr>
    <% } %>
  </table>
  <% } %>
  </center>
  </body>
</html>
